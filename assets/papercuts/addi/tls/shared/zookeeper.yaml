issuer: https://${ADDI_IP}:7600/dex
storage:
  type: sqlite3
  config:
  file: dex.db
frontend:
  theme: addi
web:
  TLSCert: conf/server_certificate.crt
  TLSKey: conf/server.key
  https: 0.0.0.0:7600
connectors: []

enablePasswordDB: true
staticClients:
  - id: refactoring-assistant
    redirectURIs:
      - https://${REFACTOR_IP}:9443
    name: Refactoring Assistant
    secret: pkce
staticPasswords:
  - email: addi_user@ibm.com
    hash:
    username: addi_user
    userID: null

